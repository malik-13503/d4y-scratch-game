# Disable package caching to prevent digest mismatch errors
cache=false
audit=false
fund=false
# Force legacy peer deps to avoid compatibility issues
legacy-peer-deps=true
# Use npm 9+ compatible configurations
engine-strict=true